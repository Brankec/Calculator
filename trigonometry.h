#pragma once
#include <math.h>

template<typename T>
T sinus(T number) { return sin(number); };
template<typename T>
T cosinus(T number) { return cos(number); };
template<typename T>
T tanges(T number) { return tan(number); };